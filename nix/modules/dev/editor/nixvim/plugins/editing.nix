{
  plugins = {
    comment = {
      enable = true;
      settings = {
        opleader.line = "gc";
        opleader.block = "gb";
        toggler.line = "gcc";
        toggler.block = "gbc";
      };
    };

    mini-ai = {
      enable = true;
    };

    vim-surround = {
      enable = true;
    };

    ts-context-commentstring = {
      enable = true;
    };
  };
}
